
name: Clone Repository

on:
  push:
    branches:
      - main

jobs:
  clone:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Move file to folder
      run: cp test_job_jenkins.py /home/ubuntu/glue-script
